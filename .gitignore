# macOS
.DS_Store

# Xcode user-specific
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Swift Package Manager
.build/
Packages/
Package.resolved
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# Build artifacts
DerivedData/
build/
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods (if ever added)
Pods/
*.xcworkspace

# Carthage (if ever added)
Carthage/Build/

# Generated by xcodegen — keep project.yml as source of truth, but the
# generated .xcodeproj is committed for now so non-xcodegen contributors
# can open the project. If we ever go xcodegen-only, ignore the .xcodeproj.
